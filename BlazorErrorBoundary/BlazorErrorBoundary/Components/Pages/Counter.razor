@page "/counter"
@using System.Diagnostics
@rendermode InteractiveServer

<PageTitle>Counter</PageTitle>

<input onkeyup="OnFKeyKeyup(event)" id="test_fkey" @onFKeyCustomEvent="resolveFkey"/>


@code {

    private void resolveFkey(FKeyEventArgs eventArgs)
    {
        Debug.WriteLine(eventArgs.SearchText);
    }

}